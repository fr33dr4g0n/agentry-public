{
  "name": "@agentry/mcp",
  "version": "0.0.1",
  "private": true,
  "description": "Agentry stdio MCP server — the canonical UX. Runs in the user's Claude Code via stdio.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "agentry-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentry/shared": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
